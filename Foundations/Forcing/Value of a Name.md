Let $(\mathbb{P},\leq,\mathbb{1})$ be a [[Forcing Partial Order]].
Fix some $F\subseteq \mathbb{P}$.
Let $\mathrm{Name}^{\mathbb{P}}$ be the class of $\mathbb{P}$-[[Names]].
Define recursively 
$$
\mathrm{val}(\tau,F) = \{ \mathrm{val}(\sigma,F) : (\exists p\in F)\, (\sigma,p)\in \tau \} 
$$
