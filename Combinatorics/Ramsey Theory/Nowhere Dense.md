A set A ⊂ X is nowhere dense if A is not dense in any non-empty open subset, i.e., Ā has empty interior.

In N(ω), Y is ?-nowhere-dense if for all A ∈ N(<ω) and M ∈ N(ω), there is L ∈ M(ω) with (A, L)(ω) ⊂ Y^c.

#ai-generated