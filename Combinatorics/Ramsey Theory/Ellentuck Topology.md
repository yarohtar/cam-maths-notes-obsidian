The Ellentuck topology or ?-topology on $[\mathbb{N}]^\omega$ has basic open sets (A, M)(ω) for A ∈ N(<ω) and M ∈ N(ω), where (A, M)(ω) = {L ∈ N(ω) : A is an initial segment of L and L − A ⊂ M}.

This is stronger than the [[τ-Topology|τ-topology]].

#ai-generated