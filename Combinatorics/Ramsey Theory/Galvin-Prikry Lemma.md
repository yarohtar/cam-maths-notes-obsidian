Given Y ⊂ N(ω), there exists a set M ∈ N(ω) such that either M accepts ∅ into Y or M rejects all of its finite subsets.

Where M accepts A if (A, M)(ω) ⊂ Y, and rejects A if no L ∈ M(ω) accepts A.

## Proof

Inductively construct M1 ⊃ M2 ⊃ · · · with certain properties.

#ai-generated